import React from 'react';
import './Hero.css';

const Hero = () => {
  return (
    <section className="hero">
      {/* Pure background image - no logo here */}
    </section>
  );
};

export default Hero;